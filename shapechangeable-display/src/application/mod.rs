pub mod image {
    use anyhow::Result;
    use embedded_graphics::prelude::Point;

    pub fn get_image(messages: Vec<u8>) -> Result<(Vec<u8>, u32, Option<Point>)> {
        unimplemented!()
    }
}
